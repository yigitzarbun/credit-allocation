//imports

const initialState = {
  user: null,
  users: [],
  sectors: [],
  occupations: [],
};
export function myReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
